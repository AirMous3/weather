import styled from 'styled-components';

export const UpcomingDaysWrapper = styled.ul`
  display: flex;
  flex-wrap: wrap;
  margin: 0;
  padding: 0;

  li {
    list-style-type: none;
    margin-left: 50px;
  }
`;
